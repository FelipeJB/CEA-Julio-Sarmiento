!function(e){"use strict";e('a.js-scroll-trigger[href*="#"]:not([href="#"])').click(function(){if(location.pathname.replace(/^\//,"")==this.pathname.replace(/^\//,"")&&location.hostname==this.hostname){var t=e(this.hash);if((t=t.length?t:e("[name="+this.hash.slice(1)+"]")).length)return e("html, body").animate({scrollTop:t.offset().top},1e3,"easeInOutExpo"),!1}}),e(".js-scroll-trigger").click(function(){e(".navbar-collapse").collapse("hide")}),e("body").scrollspy({target:"#sideNav"})}(jQuery);

function cambiarClave(){
    $('.cambiarC').css('display', 'block');
    $('.contentCambiarC').show(300);
    $('.contentEliminarS').hide();
    $('.contentAgregarS').hide();
    $('.contentEliminarP').hide();
    $('.contentAgregarP').hide();
}

function cancelarC(){
    $('.contentCambiarC').hide(300);
}

function agregarSeccion() {
    $('.agregarS').css('display', 'block');
    $('.contentAgregarS').show(300);

    $('.contentCambiarC').hide();
    $('.contentEliminarS').hide();
    $('.contentEliminarP').hide();
    $('.contentAgregarP').hide();

}

function eliminarSeccion(u){
    $('.eliminarS').css('display', 'block');
    $('.contentEliminarS').show(300);

    if($('.selS').children('option').length==0){
    for(var i = 0; i<u.length; ++i){
        $('.selS').append('<option value="'+ u[i].id +'">'+ u[i].nombre +'</option>');
    }
    }

    $('.contentCambiarC').hide();
    $('.contentAgregarS').hide();
    $('.contentEliminarP').hide();
    $('.contentAgregarP').hide();

}

function cancelarS(){
  if($('.contentAgregarS').css('display') != 'none'){
    $('.contentAgregarS').hide(300);
  }
  if($('.contentEliminarS').css('display') != 'none'){
    $('.contentEliminarS').hide(300);
  }
}

function agregarPublicacion(u) {
    $('.agregarP').css('display', 'block');
    $('.contentAgregarP').show(300);

    if($('.selAP').children('option').length==0){
    for(var i = 0; i<u.length; ++i){
        $('.selAP').append('<option value="'+ u[i].id +'">'+ u[i].nombre +'</option>');
    }
    }

    $('.contentCambiarC').hide();
    $('.contentEliminarS').hide();
    $('.contentAgregarS').hide();
    $('.contentEliminarP').hide();

}

function eliminarPublicacion(u, p){
    $('.eliminarP').css('display', 'block');
    $('.contentEliminarP').show(300);

    if($('.selP').children('option').length==0){
    for(var i = 0; i<u.length; ++i){
        $('.selP').append('<option value="'+ u[i].id +'">'+ u[i].nombre +'</option>');
    }
    //console.log(p);
    for(var i = 0; i<p[$('.selP option:first').text()].length; ++i){
        $('.selPS').append('<option value="'+ p[$('.selP option:first').text()][i].id +'">'+ p[$('.selP option:first').text()][i].nombre +'</option>');
    }
    }

    $('.contentCambiarC').hide();
    $('.contentEliminarS').hide();
    $('.contentAgregarS').hide();
    $('.contentAgregarP').hide();

}

function cancelarP(){
  if($('.addP').css('display') != 'none'){
    $('.contentAgregarP').hide(300);
  }

  if($('.delP').css('display') != 'none'){
    $('.contentEliminarP').hide(300);
  }
}

function llenarP(u){
    $('.selPS').empty();

      for(var i = 0; i<u[$('.selP').find(":selected").text()].length; ++i){
          $('.selPS').append('<option value="'+ u[$('.selP').find(":selected").text()][i].id +'">'+ u[$('.selP').find(":selected").text()][i].nombre +'</option>');
      }

}

function agregarDescripcion(){
    $('.AgregarD').css('display', 'block');
    $('.contentAgregarD').show(300);
}

function cancelarDA(){
    $('.contentAgregarD').hide(300);
}

function editarDescripcion(){
    $('.contentEliminarD').hide();
    $('.editarD').css('display', 'block');
    $('.contentEditarD').show(300);
}

function eliminarDescripcion(){
    $('.contentEditarD').hide();
    $('.eliminarD').css('display', 'block');
    $('.contentEliminarD').show(300);
}

function cancelarD(){
  if($('.contentEditarD').css('display') != 'none'){
    $('.contentEditarD').hide(300);
  }

  if($('.contentEliminarD').css('display') != 'none'){
    $('.contentEliminarD').hide(300);
  }
}
